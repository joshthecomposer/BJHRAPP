@{
    ViewData["Title"] = "Home Page";
}

<div class="min-h-screen flex justify-center">
    <div class="bg-blue-900 bg-opacity-30 rounded shadow-lg py-6 px-5 h-48 mt-5">
        <h1 class="text-5xl text-indigo-100 mb-5 font-semibold uppercase text-center">Employee Dashboard</h1>
        @await Html.PartialAsync("_Card")
    </div>
</div>