@await Html.PartialAsync("_NavBar");

<div class="mx-50 h-screen w-full top-0 p-5 bg-gradient-to-r from-[#1C2637] to-slate-900 flex flex-col gap-3">
<div class="p-10 md:p-0 flex-col gap-5 flex">
    @await Html.PartialAsync("_GreetingBar")
    <div class="ml-24 mr-5 bg-slate-800 shadow-lg px-6 pt-2 pb-4 flex flex-col gap-3">
        <div class="bg-slate-700 p-4 flex flex-col gap-2 justify-center items-center">
            <div class="p-5 flex flex-col gap-4 w-52 items-center justify-between">
                <p class="text-xl text-center capitalize">
                    Create Shift
                </p>
            </div>
            @model ShiftCheck
            <form asp-action="CreateShift" asp-controller="Shift" method="POST" class="w-full max-w-lg bg-slate-900 bg-opacity-30 rounded shadow-lg py-6 px-6">
                <div class="flex flex-wrap -mx-3 mb-6">
                    <label asp-for="UserId" class="block uppercase tracking-wide text-indigo-100 text-xs font-bold mb-2">User</label>
                    <select asp-for="UserId" class="appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500">
                        @foreach(User u in ViewBag.ShiftUsers)
                        {
                            <option value="@u.Id">@u.FirstName @u.LastName</option>
                        }
                    </select>
                    <span asp-validation-for="UserId"></span>
                </div>
        
                <div class="flex flex-wrap -mx-3 mb-6">
                    <label asp-for="ShiftDate" class="block uppercase tracking-wide text-indigo-100 text-xs font-bold mb-2">Shift Date</label>
                    <input asp-for="ShiftDate" name="ShiftDate" type="date" required class="appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"/>
                    <span asp-validation-for="ShiftDate"></span>
                </div>
        
                <div class="flex flex-wrap -mx-3 mb-6">k
                    <label asp-for="UserId" class="block uppercase tracking-wide text-indigo-100 text-xs font-bold mb-2">Block</label>
                    <select asp-for="UserId" class="appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500">
                        @foreach(var kvp in ViewBag.Blocks)
                        {
                            <option value="@kvp.Key">@kvp.Value</option>
                        }
                    </select>
                    <span asp-validation-for="Block"></span>
                </div>
        
                <div class="flex flex-wrap -mx-3 mb-6">
                    <button type="submit" class="font-bold uppercase rounded bg-slate-500 text-slate-900 px-4 py-2 w-full">
                        Create Shift
                    </button>
                </div>
            </form>
        </div>
        <div>
            <p><a href='/users/shift/all/@Context.Session.GetInt32("UserId")'>All Shifts</a></p>
        </div>
    </div>
</div>
</div>