@await Html.PartialAsync("_NavBar");
<div class="mx-50 h-screen w-full fixed z-0 top-0 bg-gradient-to-r from-[#1C2637] to-slate-900 flex flex-col gap-3 items-center justify-center">
    <div class="rounded shadow-lg bg-slate-800 p-10 flex flex-col gap-3">
        <h1 class="text-slate-50 text-5xl">Update Your Info</h1>
        @await Html.PartialAsync("_UpdateUserForm")
    </div>
</div>